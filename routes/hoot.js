import { Router} from "express";

const router = Router();

router.post("/", );
router.get("/", );
router. get("/:hootId", );
router.put("/:hootId", );
router.delete("/:hootId");
router.post("/:hootId", );

export default router;